# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license
# Balanced depth dataset: NYU real (80x) + COCO pseudo-depth (1x)
# ~182K train images (63K NYU + 118K COCO), 654 NYU val
# NYU oversampled 80x to balance with COCO volume

path: mixed_balanced  # dataset root dir (relative to Ultralytics settings 'datasets_dir')
train: images/train
val: images/val

nc: 1
names:
  0: depth

channels: 3
