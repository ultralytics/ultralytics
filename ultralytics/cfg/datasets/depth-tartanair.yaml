# TartanAir only (55,660 train / 5,810 val)
path: tartanair  # dataset root dir (relative to Ultralytics settings 'datasets_dir')
train: images/train
val: images/val
nc: 1
names:
  0: depth
channels: 3
