# YOLOA docs — evidence log

**Commit:** `38fc43475ed44fb6f150b30d45115a583ff4e0b8` (yoloa_clean, 2026-07-05 17:09:22 +0800, "add exp yamls"), local == origin.
**Env:** editable install → /Users/muratraimbekov/ultralytics, ultralytics 8.4.37, Python 3.14.0, torch 2.10.0, CPU (Apple M4).
Probes: `scratchpad/probe1_api.py` … `probe5_export.py` + inline `python3 -c` runs.

## Import / class / task string

| #   | Claim                                                                                                                                                       | Probe                                     | Observed                                                                                                                                               |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `from ultralytics import YOLOA` FAILS                                                                                                                       | probe1                                    | `ImportError: cannot import name 'YOLOA' from 'ultralytics'` (verbatim)                                                                                |
| 2   | Working import: `from ultralytics.yoloa import YOLOA`                                                                                                       | probe1                                    | OK                                                                                                                                                     |
| 3   | Task string registered = `anomaly_v2`                                                                                                                       | grep cfg/**init**.py:56 + probe1 `m.task` | `TASKS = frozenset({..., "anomaly_v2"})`; `m.task == 'anomaly_v2'`                                                                                     |
| 4   | `YOLOA()` no-arg FAILS (default `yolo26m-anomaly-v2.yaml` missing)                                                                                          | probe1                                    | `FileNotFoundError: 'yolo26m-anomaly-v2.yaml' does not exist` (verbatim)                                                                               |
| 5   | Real model yamls: `yolo26-anomaly.yaml`, `-exp.yaml`, `-maskprior.yaml` (cfg/models/v2/)                                                                    | git ls-files + ls                         | 3 files; bare name `yolo26-anomaly.yaml` resolves (warns "no model scale passed. Assuming scale='n'"); scaled `yolo26n-anomaly.yaml` resolves silently |
| 6   | Registries: TASK2DATA=coco8.yaml (placeholder comment in source), TASK2MODEL=yolo26m-anomaly-v2.yaml (file does NOT exist), TASK2METRIC=metrics/mAP50-95(B) | grep cfg/**init**.py:63,79,87             | confirmed                                                                                                                                              |
| 7   | Model class: `YOLOAnomalyV2Model(DetectionModel)`; head = plain `Detect` (AnomalyMCDetect exists in head.py but referenced by NO yaml)                      | read tasks.py:524, grep cfg/              | confirmed, `grep AnomalyMCDetect ultralytics/cfg/` → no matches                                                                                        |

## Signatures (inspect.signature, probe1)

- `__init__(self, model='yolo26m-anomaly-v2.yaml', verbose=False)`
- `fit(self, source, name=None, cache=None, refit=False, device=None, batch=8) -> YOLOA` (returns self)
- `predict(self, source=None, stream=False, **kwargs)` (+ `hm_gate_blend`)
- `val(self, validator=None, **kwargs)` (+ `end2end`, `hm_gate_blend`)
- `train/export/save` inherited from `Model`
- fit imgsz is hardcoded 640 inside `YOLOA.fit` → `build_memory_bank(source, imgsz=640, ...)`; no fit-config yaml exists — all bank knobs baked into model yaml `anomaly_v2:` block + module defaults (docstring: "not configurable per call")

## Fit flow (probe2)

| #   | Claim                                                                                                                                                                                                                     | Observed                                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 8   | fit on folder of 24 synthetic 160×160 normals                                                                                                                                                                             | 0.7 s on M4 CPU; log "Memory bank frozen: 10000 features, dim=128; temp=5.0, K=5, bank_size=10000, bb_layers=[6]" |
| 9   | bank shape (10000, 128); threshold auto-calibrated (0.9168); `fit_data='probe'`                                                                                                                                           | confirmed                                                                                                         |
| 10  | fit from list of paths works; name defaults to 'imgset'                                                                                                                                                                   | confirmed                                                                                                         |
| 11  | cache: `cache=dir` writes `<name>.pt`; 2nd fit loads instantly ("YOLOA.fit: loaded cached bank (10000 vecs)"), 0.0 s; `refit=True` forces rebuild (code path read)                                                        | confirmed                                                                                                         |
| 12  | `save()` → `YOLOA('saved.pt')` roundtrips bank; predict works with NO re-fit; bank ready True after reload                                                                                                                | confirmed                                                                                                         |
| 13  | fit on missing folder                                                                                                                                                                                                     | `FileNotFoundError: /nonexistent/folder does not exist`                                                           |
| 14  | predict BEFORE fit: silently runs vanilla detection (no warning, no prior)                                                                                                                                                | probe2, boxes (0,6)                                                                                               |
| 15  | predict AFTER fit: prior applied automatically (inferred from non-empty bank — no argument); `Results` = standard detection Results, only `.boxes` populated; internal `_last_heatmap` (1,1,80,80) not exposed on Results | confirmed                                                                                                         |

## Val flow (probe3 + inline)

| #   | Claim                                                                                                                                                                                                                                                                                             | Observed                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| 16  | Val header adds mAP10/mAP25 columns (always shown)                                                                                                                                                                                                                                                | "Box(P R mAP10 mAP25 mAP50 mAP50-95)" |
| 17  | Fitted bank → extended IoU grid incl. 0.10/0.25; metrics dict keys stay standard (precision/recall/mAP50/mAP50-95/fitness)                                                                                                                                                                        | confirmed                             |
| 18  | **BUG:** validator auto-bank-build from a plain-directory `train:` split fails (`_collect_support_paths` wraps dir in list → `IsADirectoryError: [Errno 21] Is a directory: ...`), exception swallowed silently → val silently runs WITHOUT prior. Works only via .txt train split or pre-`fit()` | inline probe                          |
| 19  | **val() RESETS the fitted bank** (`YOLOAnomalyValidator.__call__` → `memory_bank.reset()`): `ready before val: True` → `ready after val: False`; must re-fit (or use cache) before predicting again                                                                                               | probe3                                |
| 20  | `val(end2end=False)` works, head end2end restored after call (True); `val(hm_gate_blend=0.3)` works                                                                                                                                                                                               | inline probe                          |

## Train flow (probe4)

| #   | Claim                                                                                                                                                                                                                                                     | Observed                          |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| 21  | `.train(data=<standard detection yaml>, epochs=1, imgsz=320, device=cpu)` completes; trainer = `AnomalyV2RNDTrainer` (R&D default; public `AnomalyV2Trainer` exists but NOT wired — TODO in yoloa.py task_map)                                            | confirmed                         |
| 22  | loss names: box_loss, cls_loss, dfl_loss (plain Detect head)                                                                                                                                                                                              | confirmed                         |
| 23  | OOD eval skipped w/ warning when no MVTec root: "AnomalyV2RNDTrainer: no test_data_yamls or test_root configured; skipping OOD eval." Hard-coded `_MVTEC_ROOT_CANDIDATES` are personal machine paths (train_rnd.py:46-50) — research stubs, not shippable | confirmed                         |
| 24  | Prior mask during training built by `LoadAnomalyPriorMask` from bboxes (gauss render, mask_size 80) or polygons if `seg_target_polygon: true` (maskprior yaml); augmented (jitter/erase/warp/mixup, `mask_aug_passes: 2`); `p_drop: 0.5` mask dropout     | read augment.py:2270+, model yaml |

## Export (probe5)

| #   | Claim                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Observed            |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| 25  | UNFITTED export to ONNX succeeds; output `(1, 300, 6)` (end2end); ONLY standard-domain ops (domains=['']), 24 op types, no custom ops; no bank initializers                                                                                                                                                                                                                                                                                                                                                                                                                         | confirmed           |
| 26  | **FITTED export/save originally FAILED:** `RuntimeError: Only Tensors created explicitly by the user (graph leaves) support the deepcopy protocol...` — root cause: `build_memory_bank` extracted features WITHOUT `torch.no_grad()` -> bank got `requires_grad=True` (non-leaf) -> deepcopy in `Model.save`/exporter broke. Probe2's save "worked" only because an earlier predict froze params. **FIXED on this branch:** wrapped the extraction loop in `torch.no_grad()` (ultralytics/nn/tasks.py, build_memory_bank). After fix: fit->save->reload->predict OK; fit->export OK | pre/post-fix probes |
| 26a | **FITTED export embeds the bank in the ONNX graph**: initializer `[128, 10000]`, still domains=[''] (all ONNX-native), output (1, 300, 6), 14.2 MB vs 9.3 MB unfitted; runs under onnxruntime -> (1, 300, 6), all finite                                                                                                                                                                                                                                                                                                                                                            | post-fix probe      |
| 27  | Fused arch numbers (n scale, after model.fuse()): 2,563,108 params, 6.0 GFLOPs @640 (model.info); NOT benchmarks                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | probe5              |

## CLI (inline entrypoint probes)

| #   | Claim                                                                                                                                                          | Observed                                                            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| 28  | No `fit` CLI mode: MODES = {train,val,predict,export,track,benchmark}                                                                                          | `yolo fit ...` → `SyntaxError: 'fit' is not a valid YOLO argument.` |
| 29  | `yolo predict model=<fitted>.pt source=img` works (ckpt routes by baked task; CLI also routes any model with "anomaly" in stem to YOLOA — cfg/**init**.py:966) | confirmed                                                           |
| 30  | Export log suggests `yolo predict task=anomaly_v2 model=yolo26n-anomaly.onnx`                                                                                  | observed in probe5 output                                           |

## Blockers (§7) — verdicts

1. **Shippable dataset: BLOCKED.** TASK2DATA=coco8 placeholder; OOD eval roots hard-coded personal paths; no cfg/datasets/_anomaly_.yaml; no downloadable dataset → dataset example page impossible truthfully (index/format page still possible).
2. **Default model: BROKEN.** `YOLOA()` raises FileNotFoundError; every example must pass explicit yaml/ckpt.
3. **Top-level import: NOT exported.** Only `from ultralytics.yoloa import YOLOA`.
4. **Task string: `anomaly_v2` today**; checklist favours public `anomaly` — team question.
5. **Weights/benchmarks: NONE** in repo (no *.pt assets, no published tables) → Models/perf tables stay blocked.
6. **Mode limits:** fitted-model export broken (#26); val resets bank (#19); val auto-bank from dir train split silently broken (#18).

## Post-review additions

| #   | Claim                                                                                                                                                                                                                                                                                                                     | Observed                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| 31  | Two-stage review ran: spec-reviewer (2 important — extra formatter hunks in tasks.py reverted; branch-pinned link removed) + code-quality-reviewer (3 important — validator train-split auto-build claim dropped; bottle.yaml block added; anomaly_v2 no-default-model caveats added in quickstart/cli/cfg; 3 nits fixed) | agent transcripts                   |
| 32  | Exact docs `bottle.yaml` (train: train/good, val: images/val, nc: 1) verified: fit on train/good then val(data=bottle.yaml) runs, mAP10/mAP25 columns printed                                                                                                                                                             | inline probe "PASS bottle.yaml val" |
| 33  | Final gates after review fixes: house_style_probe (title soft-note only), dataset_probe clean, links 12 OK + 5 OK, 0 BROKEN                                                                                                                                                                                               | script runs                         |

## Cross-link propagation pass (pattern of PRs #24544/#24558/#24597/#24702), branch @ 8dd625908

| #   | Claim                                                                                                                                                                                            | Observed                                                      |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------- |
| 34  | Results fields on fitted checkpoint: `type(result.boxes)=Boxes`, `boxes.data` shape `(N,6)` dtype `torch.float32`, `xyxy (N,4)`, `conf (N,)`, `cls (N,)`; `masks/keypoints/probs/obb` all `None` | probe6 (predict on defect.png with yolo26n-anomaly-bottle.pt) |
| 35  | `guess_model_task` fallback warning now lists `'anomaly_v2'`; still returns `detect`                                                                                                             | inline probe on updated tasks.py                              |
| 36  | prettier warns on docs/en/index.md, help/FAQ.md, datasets/index.md are pre-existing at HEAD (verified via git stash)                                                                             | prettier --check before/after                                 |
