# SUN RGB-D only (10,335 scenes; 9,245 train / 1,090 val)
path: sunrgbd  # dataset root dir (relative to Ultralytics settings 'datasets_dir')
train: images/train
val: images/val
nc: 1
names:
  0: depth
channels: 3
