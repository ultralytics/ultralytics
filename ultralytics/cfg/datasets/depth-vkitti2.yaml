# Virtual KITTI 2 only (25,780 train / 16,740 val)
path: vkitti2  # dataset root dir (relative to Ultralytics settings 'datasets_dir')
train: images/train
val: images/val
nc: 1
names:
  0: depth
channels: 3
