# NYU Depth V2 training set (80K from HuggingFace sayakpaul/nyu_depth_v2)
# Val: standard 654-image Eigen test split (symlinked from mega_dataset)
# Purpose: NYU-domain fine-tuning after broad pretraining

path: nyu_full_hf  # dataset root dir (relative to Ultralytics settings 'datasets_dir')
train: images/train
val: images/val

nc: 1
names:
  0: depth

channels: 3
