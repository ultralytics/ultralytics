# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license
# SUN RGB-D Monocular Depth Estimation (MDE) dataset configuration
# Documentation: https://rgbd.cs.princeton.edu/
# Dataset paper: https://rgbd.cs.princeton.edu/paper.pdf
# Citation: Song et al. "SUN RGB-D: A RGB-D Scene Understanding Benchmark Suite" CVPR 2015

# Dataset path
path: sunrgbd_mde

# Train and validation paths
train: train/images
val: val/images

# Number of classes
nc: 37

# Depth range for normalization (in meters)
depth_max: 10.0

# Class names
names:
  0: bed
  1: table
  2: sofa
  3: chair
  4: toilet
  5: desk
  6: dresser
  7: night_stand
  8: bookshelf
  9: bathtub
  10: person
  11: cabinet
  12: box
  13: pillow
  14: door
  15: tv
  16: lamp
  17: bag
  18: computer
  19: monitor
  20: bin
  21: sink
  22: books
  23: curtain
  24: mirror
  25: floor_mat
  26: clothes
  27: ceiling
  28: book
  29: fridge
  30: window
  31: blinds
  32: shelves
  33: picture
  34: counter
  35: floor
  36: wall

# Download script/URL (optional)
download: |
  from pathlib import Path

  from ultralytics.data.prepare_sunrgbd_mde import prepare_sunrgbd_mde_dataset

  dataset_dir = Path(yaml["path"])
  depth_max = float(yaml.get("depth_max", 10.0))

  prepare_sunrgbd_mde_dataset(dataset_dir, depth_max=depth_max)

# Label Format:
# Each line in the label files (*.txt) contains 6 space-separated values:
#   class x_center y_center width height depth
#
# Where:
#   - class: integer class ID (0-36)
#   - x_center: normalized center x coordinate (0-1)
#   - y_center: normalized center y coordinate (0-1)
#   - width: normalized bounding box width (0-1)
#   - height: normalized bounding box height (0-1)
#   - depth: normalized depth value (0-1), calculated as depth_meters / depth_max
#            To get actual depth in meters: depth_meters = depth * depth_max
#
# Example label line:
#   3 0.5 0.5 0.15 0.25 0.35
#   (Chair at center of image, 15% width, 25% height, depth=0.35 → 3.5m with depth_max=10)

# Dataset statistics
# Training samples: ~10,335
# Validation samples: ~2,860
# Total samples: ~13,195
# Image resolution: Variable (typically 640x480 or similar)
# Depth range: 0-10 meters (typical indoor scenes)
