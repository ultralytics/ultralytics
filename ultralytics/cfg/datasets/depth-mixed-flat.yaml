# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license
# Mixed depth dataset (flat): NYU real + COCO pseudo-depth from DA V2 teacher
# ~65K train images (795 NYU + ~65K COCO), 654 NYU val

path: mixed_flat  # dataset root dir (relative to Ultralytics settings 'datasets_dir')
train: images/train
val: images/val

nc: 1
names:
  0: depth

channels: 3
